---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydeploy1
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 1
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: mycnt
          image: shaikmustafa/paytm:bus
          ports:
            - containerPort: 80
